コンポーネントの雛形を生成してください。

以下のルールに従うこと：

- 1ファイル1コンポーネント、default export
- アロー関数で作成する
- Server Component をデフォルトとし、クライアント操作が必要な場合のみ "use client" を付ける
- 型定義は `@/lib/types.ts` からインポート（必要に応じて types.ts にも型を追加）
- props の型は `Props` という名前で定義する
- className や style 属性は書かない（CSS実装は別途行う）
- 配置先は `src/components/` 以下

生成するもの：

1. コンポーネントファイル本体
2. 必要であれば `@/lib/types.ts` への型追加

生成後、実装すべきロジック部分は TODO コメントで示し、ユーザーが自分で書けるようにすること。
